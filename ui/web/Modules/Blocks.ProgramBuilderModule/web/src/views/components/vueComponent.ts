import BaseComponent from "./BaseComponent";

export class vueComponent extends BaseComponent {
    constructor(comName: string, comType: object, props: object) {
        super(comName, comType, props);
    }
}